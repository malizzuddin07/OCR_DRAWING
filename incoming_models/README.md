# Incoming Candidate Models

Place returned GPU-training ZIP files here. A returned model is a candidate only.
It must pass validation, two fresh golden-suite runs, the strict quality gate,
and human approval before production promotion.
